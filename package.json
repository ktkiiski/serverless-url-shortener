{
  "name": "url-shortener",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy:dev": "serverless deploy --stage=dev --verbose",
    "deploy:prod": "serverless deploy --stage=prod --verbose"
  },
  "dependencies": {},
  "devDependencies": {
    "serverless": "1.14.0"
  }
}
